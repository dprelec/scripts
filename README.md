## Some Useful scripts

* `dotexec.pl` - execute command with environment loaded from `.env`
* `go-imports.pl` - print imported libraries in Go files
* `mgrep.pl` - grep files via piped greps per pattern
* `sudoku.pl` - find numbers in a grid of other numbers
* `unescape.pl` - write URL-unescaped string
